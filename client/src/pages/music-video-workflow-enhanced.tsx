/**
 * Music Video Workflow Enhanced Page
 * Flujo completo integrado: Upload → Director → Script → Images → Timeline → Export
 * Mantiene el flujo anterior completo + agrega timeline profesional
 */

import { MusicVideoWorkflowComplete } from '../components/music-video/MusicVideoWorkflowComplete';

export default function MusicVideoWorkflowEnhancedPage() {
  return <MusicVideoWorkflowComplete />;
}
